def sort_strings_by_length(strings):
    return sorted(strings, key=lambda x: len(x))

strings = ["jabłko", "gruszka", "banan", "pomarańcza", "śliwka"]
sorted_strings = sort_strings_by_length(strings)
print(sorted_strings) 
